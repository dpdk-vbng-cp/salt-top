# -*- coding: utf-8 -*-
# vim: ft=jinja

{% set default = salt['grains.filter_by']({
    'default': {
      'pkgs': [
        'vlan',
        'tcpdump',
        'tmux',
        'bridge-utils',
        'iproute2',
      ],
      'kernel': {
        'modules': {
          'br_netfilter': {
          },
          'dummy': {
            'options': {
              'numdummies': '4',
            },
          },
        }
      },
    },
  }) %}
